services:
  _defaults:
    autowire: true
    autoconfigure: true

  GAYA\Taxonomy\:
    resource: '../Classes/*'

  # Events
  GAYA\Taxonomy\Tca\Registry:
    tags:
      - name: event.listener
        identifier: 'gaya-taxonomy-tca-registry'
        method: 'addTaxonomyDatabaseSchema'

  # DataProcessor
  GAYA\Taxonomy\DataProcessing\TaxonomyProcessor:
    shared: false
    tags:
      - name: 'data.processor'
        identifier: 'taxonomy'
